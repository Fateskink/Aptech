class Product {
  final String name;
  final String url;
  final double price;
  final String hexColor; // Added field
  Product({
    required this.name,
    required this.url,
    required this.price,
    required this.hexColor, // Added parameter
  });
}